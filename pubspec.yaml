name: maps_brasilia_osm_camera
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.0.0 # Map
  latlong2: ^0.9.0 # Manipulate the coordenates
  geolocator: ^11.0.0 # Get the localization of the user
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
